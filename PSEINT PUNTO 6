Algoritmo CalculoF
    Definir x como Entero
    Definir sigma como Real
    Definir y como Entero
    Definir lambda como Real
    Definir alfa como Real
    Definir f como Real
    x <- 12
    sigma <- 2.1836
    y <- 3
    lambda <- 1.11695
    alfa <- 328.67
    f <- 3 * x * ((x + sigma * y) / (x * x - y * y)) - lambda * (alfa - 13.7)
    Escribir "El valor de f es: ", Redondear(f, 2)
FinAlgoritmo
